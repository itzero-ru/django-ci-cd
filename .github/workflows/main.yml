name: CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run a multi-line script
        run: |
          ssh test-srv.itzero.ru /bin/bash <<'EOT'
          #sudo apt-get install mc -y
          ls
          #sudo ping -c 2 8.8.8.8
          
