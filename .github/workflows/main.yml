name: CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run a multi-line script
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SECR }}
        run: |
          echo $SSH_PRIVATE_KEY
          #ssh test-srv.itzero.ru >log.txt
          #cat log.txt
          #sudo apt-get install mc -y
          ls
          #sudo ping -c 2 8.8.8.8
          
